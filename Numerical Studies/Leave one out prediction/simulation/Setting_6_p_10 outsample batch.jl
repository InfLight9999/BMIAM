const SETTING=6
const DIM=10
const DESIGN="IndNorm"
# Select the datanum th replication simulation data (datanum=3,2,...,100)
const datanum=1
# Number of intervals of B-spline basis function (This should match the 'bsMat')

include("./out sample simulation batch.jl")
